{
  "name": "duo-pp",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test --test-concurrency=1 tests/*.test.mjs",
    "test:webkit": "DUO_WEBKIT=1 node --test --test-name-pattern=WebKit tests/interaction.test.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "typescript": "~5.9.2",
    "vite": "^7.1.5"
  }
}
